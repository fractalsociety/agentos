# Companion Gateway (host boundary)

**Issue:** `fos-gz0.14.17`

Host/runtime proof for the Fractal companion gateway boundary lives in:

- `kernel/fractalos-root-task/include/companion_gateway.h`
- `kernel/fractalos-root-task/src/companion_gateway.c`
- `tests/test_companion_gateway.c` (xtask suite `test_companion_gateway`)

This directory is a placeholder for a future Microkit PD packaging of the same
contract. **No HTTP, TLS, HTML, CSS, or JavaScript belongs here.** Page
rendering is the sibling `fractalos-companion` repository (`fos-gz0.14.18`).
