[package]
name = "fractal-runtime"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Deterministic Agent IR → capability-scoped WASM compiler and AOT cache"
publish = false

[dependencies]
sha2 = "0.10"
thiserror = { workspace = true }
wasmi = "0.31"

[dev-dependencies]
tempfile = "3"
