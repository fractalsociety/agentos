[package]
name = "fractalos-tool-registry"
version = "0.1.3"
edition = "2021"
description = "Tool Registry - capability-gated tool registration and dispatch for FractalOS"

[features]
std = []

[dependencies]
fractalos-sdk = { path = "../../sdk" }
